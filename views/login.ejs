<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>تسجيل الدخول</title>
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="bg-white py-3 border-bottom shadow-sm">
    <div class="container text-center">
      <h2 class="fw-semibold mb-1">
         تخمين ملف تعريف الارتباط للبقاء قيد تسجيل الدخول
      </h2>
      <p class="text-muted small mb-0">Brute-forcing a stay-logged-in cookie</p>
    </div>
  </div>
  

  <div class="container mt-5">
    <div class="row justify-content-center">

      <div class="col-md-6 d-none d-md-block">
  <div class="bg-white p-4 shadow-sm rounded h-100">
    <h4 class="mb-4 fw-bold">معلومات مهمة</h4>
    <ul class="list-unstyled fs-5 lh-lg">
      <li><strong>معلومات تسجيل الدخول:</strong> yasser:debihi28</li>
      <li><strong>إسم مستخدم الضحية:</strong> said</li>
      <li>
        <strong>لائحة كلمات المرور المتوقعة:</strong>
        <a href="/file/passwords.txt" download class="btn btn-sm btn-outline-secondary ms-2">تحميل الملف</a>
      </li>
    </ul>
  </div>
</div>


      <div class="col-md-6 col-lg-4">
        <div class="card p-4 shadow-sm">
          <h2 class="text-center mb-4">تسجيل الدخول</h2>
          <!-- Error Message Section -->
          <% if (typeof error !== "undefined") { %>
            <div class="alert alert-danger text-center mb-4">
              <%= error %>
            </div>
          <% } %>

          <!-- Login Form -->
          <form method="POST" action="/login">
            <div class="mb-3">
              <label for="username" class="form-label"> إسم المستخدم</label>
              <input name="username" id="username" type="text" class="form-control" required />
            </div>

            <div class="mb-3">
              <label for="password" class="form-label">كلمة المرور</label>
              <input name="password" id="password" type="password" class="form-control" required />
            </div>

           <input type="checkbox" id="remember" name="remember"/>
           <span style="margin-right: 4px;">
            
                تذكرني
           </span>
              
            

            <div class="d-grid mt-3">
              <button type="submit" class="btn btn-primary mb-2">تسجيل الدخول</button>
            </div>
          </form>

        </div>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      var alertBox = document.querySelector(".alert-danger");
      if (alertBox && alertBox.textContent.trim() === "") {
        alertBox.style.display = "none";
      }
    });
  </script>
  
</body>
</html>
